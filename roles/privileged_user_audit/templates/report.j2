Privileged User Access Audit Report
==================================

Date: {{ ansible_date_time.date }} {{ ansible_date_time.time }}

Users with UID 0 (root or equivalent) and sudo access:
------------------------------------------------------

{% for user in privileged_users %}
User: {{ user }}
Home Directory: {{ user_homes[user] }}

Authorized SSH Key Info:
{% if ssh_key_info[user].exists %}
  Exists: Yes
  Permissions (mode): {{ ssh_key_info[user].mode }}
  Owner: {{ ssh_key_info[user].owner }}
{% else %}
  No authorized_keys file found.
{% endif %}

Sudo Privileges (sudo -l output):
---------------------------------
{{ sudo_l_output[user] | default('No sudo privileges or command output available.') }}

Files Owned on System:
---------------------
{% if user_files[user] %}
{% for line in user_files[user] %}
  {{ line }}
{% endfor %}
{% else %}
  No files found owned by {{ user }} or access denied.
{% endif %}

------------------------------------------------------
{% endfor %}

End of Report.