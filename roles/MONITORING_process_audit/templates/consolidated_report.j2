Hello Security Team,

This is the **CONSOLIDATED** daily process audit report for
{{ aggregated_results | length }} host(s) reporting new processes,
executed on {{ ansible_date_time.date }}.

---

{% if aggregated_results | length == 0 %}

## ✅ NO NEW PROCESSES DETECTED

No new process executions were detected on any monitored host
during this audit window.

### 🔍 Systems Checked ({{ scanned_hosts | length }})

{% for host in scanned_hosts %}
- {{ host }}
{% endfor %}

This confirms that no previously unseen executables were launched
since the last audit cycle.

{% else %}

## 🚨 HOSTS REPORTING NEW PROCESSES

{% for host, processes in aggregated_results.items() %}
### ➡️ Host: {{ host }}

* Total New Processes Detected: **{{ processes | length }}**

{% for proc in processes %}
- {{ proc }}
{% endfor %}

---
{% endfor %}

{% endif %}

## 💡 Ansible Execution Details

* **Audit Key:** {{ audit_rule_key }}
* **Report Generated On:** {{ ansible_date_time.date }} at {{ ansible_date_time.time }}
* **Reported By:** {{ ansible_hostname }} (Ansible Control Node)

---

This consolidated report was generated by the automated security
audit system. Please investigate any unexpected processes.
