Sudo Privilege Report for {{ ansible_hostname }}
This report was generated on {{ ansible_date_time.iso8601_micro }}.

Findings
The following users were identified as having sudo privileges on the system:

{% if sudo_users | length > 0 %}
{% for user in sudo_users %}

User: {{ user }}
{% endfor %}
{% else %}

No users with sudo privileges were found.
{% endif %}

Technical Details
This report was generated by inspecting the contents of the following files:

/etc/sudoers

Files in /etc/sudoers.d/

Note: This analysis relies on the presence of user or group names within the sudoer configuration files. It may not account for all possible, highly-complex sudoer configurations.